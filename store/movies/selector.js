// export const selectMovies = (state) => state.movies.movies;
export const selectGame = (state) => state.movies.game;
export const selectMoviesGames = (state) => state.movies.gameMovies;
export const SelectCommonMovies = (state) => state.movies.gameMovies;
