import React, { useEffect } from "react";
import { Text, View, ScrollView, Image, StyleSheet, Card } from "react-native";

export default function ThirdPage({ route }) {
  return (
    <View>
      <Card>card</Card>
    </View>
  );
}
