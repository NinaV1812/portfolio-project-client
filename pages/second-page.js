import React, { useRef } from "react";
import { Text, View, ScrollView, Image, StyleSheet } from "react-native";

export default function SecondPage({ genres }) {
  console.log("genres", genres);
  return (
    <View>
      <Text>Hello Nina!</Text>
    </View>
  );
}
